int fib(int x){
    if(x < 3){
        return 1;
    }
    else{
        return fib(x-1) + fib(x-2);
    }
}

int f(int n){
int sum;
int i;
sum = 0;
i=n;
  while(i>0){
   sum = sum + i;
   i = i - 1;	
}
return sum;
}

int g(int x,int flag){
if(flag==0)return fib(x);
else return f(x);
}
